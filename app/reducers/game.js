// @flow

const defaultState = {
  name: null,
  id: null,
  tasks: [],
  players: [],
  status: null,
};

const reducer = (state: Object = defaultState, action: Function) => {
  return state;
};

export default reducer;
