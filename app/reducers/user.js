// @flow

const defaultState = {
  guid: null,
  name: null,
  pickedCard: null,
  isReady: false,
  isOffline: false,
  loggedIn: false,
};

const reducer = (state: Object = defaultState, action: Function) => {
  return state;
};

export default reducer;
